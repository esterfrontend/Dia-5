const form = {
  name: '',
  phone: 0,
  email: null
};

// Generar una función que compruebe que las propiedades del obj "form" tengan valor. Las propiedad que tiene que mirar son:
/**
 * name
 * phone
 * email
 * age
 */

// Generar una función por cada uno de las propiedades del obj que se encargue de modificarla.

// Generar una función que recibe dos parámetros (key, value), donde la "key" será una propiedad del obj "form" y el "value" será el nuevo valor.
// No debería de poder añadir una propiedad diferente a:
/**
 * name
 * phone
 * email
 * age
 */

// Modificar las funciones anteriores que modifican cada una de las propiedades reutilizando la función que puede modificar cualquier propiedad.