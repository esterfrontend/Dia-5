/**
 * Generara un obj que tenga los metodos:
 * - suma: que sume dos números
 * - resta: que reste dos números
 * - multiplicar: que multiplique tres números
 * - dividir: que divida dos números (no puede dar error)
 */


/**
 * Además tenemos que:
 * - resta: si la resta es negativa, tendrá que devolver 0.
 * - multiplicar: No se pueden multiplicar números por 0.
 * - suma: si el resultado es divisible entre 7 tendrá que multiplicarse por 2, si es divisible entre 5 tendrá que restar 1,
 * si es divisible entre 7 y 5 se tendrá que dividir entre 35 y si no cumple ninguna, devolverá el número multiplicado
 * (recordar que para que un número sea divisible es que su resto sea 0)
 */